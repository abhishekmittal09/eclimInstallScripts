eclimInstallScripts
===================

1. When running createProjs.sh make sure that that the paths of ruby interpreter is properly set and the path of supported languages corresponds to the directory in which you wish to setup the languages

2. When running install.sh, the installDir is where eclipse and eclim will get downloaded and installed.
